cmake_minimum_required( VERSION 3.11 )
project(Kaleidoscope)
file(GLOB header_files header/*.hpp)
file(GLOB source_files source/*.cpp)
add_executable(a.exe ${source_files} ${header_files})
set(llvm_header_path D:/msys2/mingw64/include)
include_directories(${llvm_header_path} header)